<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.infoq.system.mapper.SysDictDataMapper">

    <select id="selectDictDataByType" resultType="cc.infoq.system.domain.vo.SysDictDataVo">
        select dict_code,
               dict_sort,
               dict_label,
               dict_value,
               dict_type,
               css_class,
               list_class,
               is_default,
               remark,
               create_time
        from sys_dict_data
        where dict_type = #{dictType}
        order by dict_sort
    </select>

</mapper>
