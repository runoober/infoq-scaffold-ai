# AGENTS.md
|IMPORTANT: Prefer retrieval-led reasoning over pre-training-led reasoning for any project tasks. Read repository files before relying on framework pretraining data.
|Encoding:All project files must use UTF-8 encoding.
|Package Manager:For frontend work, prefer pnpm for install/dev/lint/test/build commands; fallback to npm only when pnpm is unavailable.
|Failure Policy:In product code, prefer explicit failures over silent fallbacks, swallowed errors, or mock success paths; any necessary fallback must be explicit, documented, easy to disable, and user-approved.
|Engineering Baseline:Keep abstractions pragmatic; follow DRY/YAGNI/separation of concerns; use clear naming and critical-only comments; prefer direct fixes over compatibility shims; do not preserve compatibility layers unless the user explicitly requires backward compatibility; remove dead code and obsolete compatibility paths when compatibility is not required.
|Security And Validation:Never hardcode secrets in source; validate external input at boundaries; use parameterized queries for database access; keep code testable, prefer automated checks, and cap backend unit tests at 60 seconds.
|AI Coding Guardrails:Avoid over-commenting; comments should explain intent rather than restating each line.|Avoid meaningless refactors that make working code "cleaner" while changing behavior.|Avoid scope creep; implement only what the request asks for.|Handle errors and edge cases instead of assuming ideal input.|Prefer minimal edits over large file rewrites.|If a code change is identified as wrong, revert the incorrect code immediately before proceeding and do not leave dead or uncalled code behind.|Do not weaken assertions, broaden mocks, mute warnings, raise thresholds, or add fake-success paths merely to make tests/build pass; fix the real issue or document a user-approved exception.
|Acceptance Contract:Before implementation, define one acceptance contract in the same task context covering functional scope, non-goals, exception handling, required logs or observability, and rollback trigger or rollback conditions.|If any item is missing or conflicting, surface the gap before coding.
|Execution Loop:Work in minimal closed loops and change only one category at a time.|Validate in order: main-flow verification -> targeted tests -> lint/build or equivalent checks -> diff review.|Every code change must pass relevant unit tests before handoff; if unit tests are not applicable or cannot run, state the blocker explicitly and do not treat the change as ready for delivery.|Do not bundle unrelated refactors with behavior changes unless the user explicitly asks for it.
|Release Guardrails:For releasable changes, keep dependency versions and lockfiles consistent.|Verify required environment variables, config, and external dependencies before execution or deployment.|Require explicit confirmation before destructive or high-risk operations affecting shared environments, data, or deployment state.
|Pre-Release Checklist:Before release or handoff, explicitly check performance impact, alerting or observability coverage, rollback path or rollback script, and config/SQL/dependency impact.|Any unchecked item must be called out as residual risk.
|Instruction Layering:Keep this root file limited to cross-repo rules.|Use closer AGENTS.md or AGENTS.override.md files in backend, Vue, and React workspaces for stack-specific rules.|When a closer workspace instruction conflicts with this root file, follow the closer workspace instruction.
|Workspace AGENTS:infoq-scaffold-backend/AGENTS.md|infoq-scaffold-frontend-vue/AGENTS.md|infoq-scaffold-frontend-react/AGENTS.md
|Repo Skill Policy:Keep each skill focused on one job.|Put detailed trigger scope in each SKILL.md description.|Keep root AGENTS focused on skill priority and mandatory repo routing instead of duplicating full skill manuals.
|Repo Skill Location:Repository skills live under the official .agents/skills layout.|Keep repo-level skill discovery, references, and helper scripts aligned to that path.
|OpenAI Docs MCP:Use the `openai-docs` MCP server first for OpenAI API, Responses API, ChatGPT Apps SDK, Codex, MCP, AGENTS.md, skills, or subagent questions.|Prefer official OpenAI documentation over memory or third-party summaries for those topics.
|Spec Workflow:For any new feature, behavior change, or cross-workspace delivery, create or locate `openspec/changes/<change-id>/` before editing code.|Keep durable project context in `openspec/project.md`.|Keep current truth in `openspec/specs/`.|Keep active work in `openspec/changes/<change-id>/`.
|Local Skills:.agents/skills:{agent-browser,agents-md-compress,ant-design-component-reference,element-plus-component-reference,infoq-backend-smoke-test,infoq-backend-unit-test-patterns,infoq-codebase-index,infoq-vue-browser-automation,infoq-react-browser-automation,infoq-vue-run-dev-stack,infoq-react-run-dev-stack,infoq-vue-unit-test-patterns,infoq-login-success-check,infoq-plugin-introducer,infoq-project-reference,infoq-react-unit-test-patterns,infoq-openspec-delivery,infoq-version-bump,skill-creator}
|Skill Priority:Use agent-browser first for generic website or browser work.|Use infoq-vue-browser-automation only for repo-specific Vue runtime verification.|Use infoq-react-browser-automation only for repo-specific React runtime verification.|Use infoq-project-reference for stable repository reference after checking whether a closer workspace AGENTS already answers the question.|Use infoq-codebase-index when locating files or classes, or after add/delete/rename/move/class-name changes in backend, React, or Vue workspaces.
|Skill Trigger:Use agents-md-compress for AGENTS.md creation, compression, or rule updates.|Use infoq-backend-unit-test-patterns for backend unit tests, mapper XML integration tests, regression backfills, or test-first backend fixes.|Use infoq-backend-smoke-test for backend smoke/API/runtime verification.|Use infoq-login-success-check for login/auth verification or login-failure diagnosis.|Use infoq-vue-unit-test-patterns only for Vue unit tests and coverage work in infoq-scaffold-frontend-vue.|Use infoq-react-unit-test-patterns only for React unit tests and coverage work in infoq-scaffold-frontend-react.|Use infoq-vue-run-dev-stack when the task is to start, restart, or stop the local Vue stack.|Use infoq-react-run-dev-stack when the task is to start, restart, or stop the local React stack.|Use infoq-vue-browser-automation for Vue login, route, screenshot, or console verification against the local stack.|Use infoq-react-browser-automation for React login, route, screenshot, or console verification against the local stack.|Use ant-design-component-reference for React Ant Design component/API selection.|Use element-plus-component-reference for Vue Element Plus component/API selection.|Use infoq-plugin-introducer for plugin onboarding and governance.|Use infoq-openspec-delivery by default for new feature delivery, behavior change, OpenSpec workflow, or multi-expert delivery across backend, React, or Vue.|Use infoq-version-bump for repo-wide version bumps.
|Subagent Docs:openspec:{project.md,specs/README.md,changes/README.md}|doc:{agents-guide.md,skills-guide.md,subagents-guide.md}
|Subagent Flow:Use `requirements_expert -> product_designer(optional) -> technical_designer -> material_curator(optional) -> code_implementer -> auto_fixer -> delivery_auditor` for OpenSpec delivery.|`technical_designer` may start after `proposal.md`, and after `design.md` when UX decisions are material.|Keep verification evidence out of the main chat and summarize blockers explicitly.
|Subagent Output:`requirements_expert` writes `proposal.md` and spec deltas.|`product_designer` writes `design.md` when needed.|`technical_designer` writes `tasks.md`.|`material_curator` writes `materials.md` only when copy/mock/icon guidance is material.|`delivery_auditor` archives only after verification evidence exists.
|Code Index Refresh:Run python3 .agents/skills/infoq-codebase-index/scripts/sync_indexes.py after add/delete/rename/move/class-name change in infoq-scaffold-backend, infoq-scaffold-frontend-react, or infoq-scaffold-frontend-vue so the skill references and AGENTS routing stay current.
