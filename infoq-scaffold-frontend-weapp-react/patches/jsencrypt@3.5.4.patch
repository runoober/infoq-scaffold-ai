diff --git a/lib/lib/jsbn/rng.js b/lib/lib/jsbn/rng.js
index f8f4643bb0b8851c1421a7a5f8f8d68f3ee31b6f..be82f95a98b6855fc85913e6421a2e17bb71fda2 100644
--- a/lib/lib/jsbn/rng.js
+++ b/lib/lib/jsbn/rng.js
@@ -3,15 +3,16 @@ import { prng_newstate, rng_psize } from "./prng4";
 var rng_state;
 var rng_pool = null;
 var rng_pptr;
+var runtimeSelf = typeof self !== 'undefined' ? self : undefined;
 // Initialize the pool with junk if needed.
 if (rng_pool == null) {
     rng_pool = [];
     rng_pptr = 0;
     var t = void 0;
-    if (typeof window !== 'undefined' && self.crypto && self.crypto.getRandomValues) {
+    if (typeof window !== 'undefined' && runtimeSelf && runtimeSelf.crypto && runtimeSelf.crypto.getRandomValues) {
         // Extract entropy (2048 bits) from RNG if available
         var z = new Uint32Array(256);
-        self.crypto.getRandomValues(z);
+        runtimeSelf.crypto.getRandomValues(z);
         for (t = 0; t < z.length; ++t) {
             rng_pool[rng_pptr++] = z[t] & 255;
         }
@@ -22,11 +23,11 @@ if (rng_pool == null) {
     var onMouseMoveListener_1 = function (ev) {
         count = count || 0;
         if (count >= 256 || rng_pptr >= rng_psize) {
-            if (self.removeEventListener) {
-                self.removeEventListener("mousemove", onMouseMoveListener_1, false);
+            if (runtimeSelf && runtimeSelf.removeEventListener) {
+                runtimeSelf.removeEventListener("mousemove", onMouseMoveListener_1, false);
             }
-            else if (self.detachEvent) {
-                self.detachEvent("onmousemove", onMouseMoveListener_1);
+            else if (runtimeSelf && runtimeSelf.detachEvent) {
+                runtimeSelf.detachEvent("onmousemove", onMouseMoveListener_1);
             }
             return;
         }
@@ -40,11 +41,11 @@ if (rng_pool == null) {
         }
     };
     if (typeof window !== 'undefined') {
-        if (self.addEventListener) {
-            self.addEventListener("mousemove", onMouseMoveListener_1, false);
+        if (runtimeSelf && runtimeSelf.addEventListener) {
+            runtimeSelf.addEventListener("mousemove", onMouseMoveListener_1, false);
         }
-        else if (self.attachEvent) {
-            self.attachEvent("onmousemove", onMouseMoveListener_1);
+        else if (runtimeSelf && runtimeSelf.attachEvent) {
+            runtimeSelf.attachEvent("onmousemove", onMouseMoveListener_1);
         }
     }
 }
